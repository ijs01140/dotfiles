# Take a screenshot with all output and save it into screenshots
bindsym Print exec grim ~/screenshot-$(date +%Y-%m-%d_%H-%m-%s).png

# Take a Screenshot with the region select (use slurp)
bindsym $mod+Print exec grim -g "$(slurp)" ~/screenshot-$(date +%Y-%m-%d_%H-%m-%s).png
